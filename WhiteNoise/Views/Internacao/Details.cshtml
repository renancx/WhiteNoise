@model WhiteNoise.Models.Internacao.InternacaoGridModel

<div>
    <h2>Detalhes da Internacao</h2>
    <div>
        <dl class="row">
            <dt class="col-sm-2">Data de Entrada</dt>
            <dd class="col-sm-10">@Model.DataEntrada</dd>

            <dt class="col-sm-2">Paciente</dt>
            <dd class="col-sm-10">@Model.Paciente</dd>

            <dt class="col-sm-2">Leito</dt>
            <dd class="col-sm-10">@Model.Leito</dd>

            <dt class="col-sm-2">Observação</dt>
            <dd class="col-sm-10">@Model.Leito</dd>

            <dt class="col-sm-2">Paciente</dt>
            <dd class="col-sm-10">@Model.Paciente</dd>

            <dt class="col-sm-2">Data de Alta</dt>
            <dd class="col-sm-10">@Model.DataAlta</dd>
        </dl>

        <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary">Editar</a>
        <a asp-action="Index" class="btn btn-secondary">Voltar</a>
    </div>
</div>
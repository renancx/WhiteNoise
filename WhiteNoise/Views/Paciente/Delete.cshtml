@model WhiteNoise.Models.Paciente.PacienteGridModel

<div class="container">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2><i class="bi bi-exclamation-triangle-fill me-2"></i> Excluir Paciente</h2>
    </div>

    <div class="alert alert-danger" role="alert">
        <h4 class="alert-heading">Tem certeza desta ação?</h4>
        <p>Esta ação é irreversível e removerá permanentemente o registro selecionado.</p>
    </div>

    <div class="card border-danger mb-4">
        <div class="card-header bg-danger text-white py-3">
            <h5 class="card-title mb-0">Dados do Paciente</h5>
        </div>
        <div class="card-body">
            <div class="row">
                <div class="col-md-8">
                    <div class="d-flex align-items-center mb-3">
                        <div>
                            <h6 class="text-muted mb-0">Nome Completo</h6>
                            <p class="mb-0 fs-5">@Model.Nome</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="d-flex align-items-center mb-3">
                        <div>
                            <h6 class="text-muted mb-0">Data de Nascimento</h6>
                            <p class="mb-0">@Model.DataNascimento.ToString("dd/MM/yyyy")</p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="d-flex align-items-center mb-3">
                        <div>
                            <h6 class="text-muted mb-0">Estado Clínico</h6>
                            <p class="mb-0">@Model.EstadoClinico</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-6">
                    <div class="d-flex align-items-center mb-3">
                        <div>
                            <h6 class="text-muted mb-0">Status</h6>
                            <p class="mb-0">
                                <span class="badge" >
                                    @(Model.Ativo ? "Ativo" : "Inativo")
                                </span>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <form asp-action="DeleteConfirmed" method="post" class="w-25">
        <input type="hidden" asp-for="Id" />
        <div class="d-flex justify-content-end">
            <button type="submit" class="btn btn-danger  w-50">
                <i class="bi bi-trash me-1"></i> Excluir
            </button>
            <a asp-action="Index" class="btn btn-outline-secondary ml-2 w-50">
                <i class="bi bi-x-circle me-1"></i> Cancelar
            </a>
        </div>
    </form>
</div>